# Redis 配置（Phase 1 单实例）

# AOF 持久化：最多丢 1 秒数据
appendonly yes
appendfsync everysec

# 密码（Phase 1 开发环境暂不设密码，Phase 2 加 requirepass）
# requirepass your_password_here

# 最大内存限制（256MB，够用）
maxmemory 256mb
maxmemory-policy allkeys-lru

# 日志级别
loglevel notice
