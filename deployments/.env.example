# 复制为 .env 并填入实际值后再启动
# cp .env.example .env

# 数据库
POSTGRES_PASSWORD=zhuzhao_dev

# Redis（F-18：生产务必覆盖为强密码）
REDIS_PASSWORD=zhuzhao_dev

# JWT（必填，请使用 openssl rand -base64 32 生成）
JWT_SECRET=
JWT_ACCESS_TTL=30m
JWT_REFRESH_TTL=168h

# 对外端口
APP_PORT=33333
