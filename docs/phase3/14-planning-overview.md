# 14 - Phase 3 规划整理（现行主链版）

> **定位**：§21–§24 全部拍板后的 **Phase 3 规划现状整理稿**——把散落在 design-decisions（§21/§22/§23/§24）、13-implementation-plan、00-startup-checklist、phase3/README 里的规划信息归拢成一份可读的现状视图。**本文档是索引/视图，不是第二 SSOT**；每节的权威出处均已标注。
> **状态**：规划整理（2026-09-02）。**SSOT** = [design-decisions §21–§24](../design/design-decisions.md)（决策）+ [13-implementation-plan](./13-implementation-plan.md) §1（排期）。
> **标记约定**：`🚦` = 触发条件驱动，是否纳入本次排期由所有者决定；`⚠️` = 存在不确定性/待拍板；`~~划线~~` = 暂缓 / 被取代（历史保留）。

---

## 〇、规划现状一句话

**工单自研暂缓（内部引擎优先，自研兜底）**：项目将迁移公司内部并对接内部工单平台/引擎，Phase 2 工单现状封版（仅保 BK-20 数据安全修复）。Phase 3 主线改为**通用能力 + 内网迁移**：

```
M0 启动准备 → M-E 任务平台 → M-A activelist 独立实现 → M-HR HR 同步 → M-SSO 单点登录（🚦）→ M-Mig 迁移准备（🚦）
```

zhuzhao 价值重定位 = **IAM 内核 + 通用能力底座（事件/审计/组织/HR/SSO）+ activelist**；工单从「自研产品线」转为「公司平台对接方」（design-decisions §23.4）。

> **翻案条件（§23）**：公司内部平台无法承接工单诉求（功能/流程/集成成本），或迁移计划取消 → 恢复自研路线（10 号设计 SSOT 完整保留，随时可重启）。

### 关键数字

| 项 | 值 |
|---|---|
| 主链合计 | **10–15 人日** = M0 1–2 + M-E 6–8 + M-HR 3–5（不含 🚦；M-A 收窄后约 2–4 ⚠️ 仍单列） |
| 主链段数 | 6 段（M0 → M-E → M-A → M-HR → M-SSO → M-Mig） |
| 🚦 段 | M-SSO / M-Mig / M1（随触发条件推进） |
| 暂缓项 | M2 工单业务闭环（自研暂缓）、M1 可运维基座（🚦）、M4、M5 |
| 文档就绪 | Phase 3 文档全量就绪（00–14 + ops/deployment） |
| 迁移 | 启动时按 A2 规则重排，Phase 3 起 000020（当前 000001–000019 已占用） |

---

## 一、现行主链里程碑（SSOT：13-implementation-plan §1）

| 里程碑 | 内容 | 人日 | 退出标准 |
|---|---|---|---|
| **M0 启动准备**（收窄） | 迁移号核对重排 + 决策清单过表 + **BK-20**（禁删有未结工单组织守卫——工单封版前最后的数据安全修复）；BK-19 / 顺手项随封版后置 | 1–2 | 决策清单过完；BK-20 落地（ErrOrgHasOpenTickets 409 + acceptance 适配） |
| **M-E 事件与任务平台** | ① **Asynq 底座**（Scheduler / PeriodicTask / worker / 重试 / 阻塞策略按任务拍板）；② **预置任务**（审计归档 B11② 首个，非工单闭环独立验收）；③ **自定义脚本任务**（job_configs 配置表 + `os/exec` Python/Shell 执行器 + job_runs 执行记录 + 管理 CRUD/启停/手动触发/历史端点；**仅全局管理员可配**；参照 ginfast scheduler / xxl-job GLUE，§23 补充拍板） | 6–8 | 归档任务按周期跑通；脚本任务可配置/执行/查历史（管理员侧）；全门禁绿 |
| **M-A activelist 独立实现** | 独立库 + **独立数据库**（§22.3/§23）；**外部事件接入契约由 activelist 侧定义**（工单非首数据源，§23.2——取代 ActivelistWriter 方向）；**2026-09-03 职责收敛**（ADR-003 修订）：收窄为**动态数据模型平台**（类型注册/Schema 演进/动态校验/CRUD/存储），事件与审计移交 zhuzhao（Asynq 显式发布 / zhuzhao 侧记录），进程 3→1，独立部署保留；**前置：共享 utils 抽取**（🚦） | 约 2–4（⚠️ 待校准；原单估） | 按其项目自身验收 |
| **M-HR HR 同步**（预留接口版） | `HRFetcher` 接口 + sync 引擎 + 本地 mock adapter（内网 adapter 即插）；拍板：离职在途工单处置 / 部门撤销×tickets.org_path 级联 / 跨部门权限分配规则 | 3–5 | 组织同步三规则对 mock 源跑通；对账幂等；三拍板项落档 |
| **M-SSO 单点登录**（🚦 进内网后实施） | `SSOProvider` 接口 + OAuth2.0 授权码实现（authorize + code 换 token + userinfo，state 防 CSRF）+ `/auth/sso/login`·`/auth/sso/callback` 两端点 + 身份映射（对账键同 HR：external_id 优先）+ 登录审计 method 字段；**鉴权三层零改动**（callback 签发自有 JWT/RT）；JIT 默认关（config 开关，仅限已同步账号）；本地密码兜底并存（§24） | 🚦 2–3 | mock/测试 IdP 走通完整回调链 → 自有 JWT 签发 → 三层鉴权行为不变 |
| **M-Mig 迁移准备**（🚦） | 公司内网部署对接（网络/凭据；07 security 与 08 ops 中生产相关项按内网形态重估：CORS 收紧 / 限流 / 审计保留期）+ **部署镜像带 python/shell 解释器**（任务平台运行时依赖，§23）+ 与 M-SSO 汇合（拿到公司 OAuth2.0 接入信息后实施） | 🚦 随迁移时点 | 内网环境部署演练通过 |

---

## 二、暂缓 / 后置（随 §23 封版）

- **~~M2 工单业务闭环~~**（7a–7e、BranchedStateEngine、B3 推进端点、审批/报表前端）**全部暂缓自研**；10/12 号转**对接参考与历史设计**（13 §4 整节保留）。工单对接形态（IAM 提供方 / 数据同步 / 单点跳转）与 Phase 2 资产处置（保留兜底 / 适配层 / 逐步下线）= **迁移时拍板 🚦**（§23.3）。
- **~~M1 可运维基座~~**（🚦，§22.1）：单实例无验收手段（MI1–5 需 2 实例 + Nginx），随部署形态升级触发；**B11① 判定日志价值独立于多实例，可与 M-E 合并实施**。
- **~~M4 activelist 集成~~**：被 **M-A** 取代（§23.2）；**~~M5 3b~~**：全部 🚦。
- **~~BK-19 / 顺手项~~**（工单 handler 测试、BK-9/F-31④/F-32/TC-2/3/4/Q5）随工单封版后置；**审批流引擎选型**随 §23 暂缓（00 §3 / 13 §8）。

---

## 三、🚦 触发条件驱动项总表（由你决定，13 §8）

| 项 | 触发条件 | 状态 |
|---|---|---|
| M1 可运维基座（observability / multi-instance / audit-l2） | 部署形态升级为多实例 / 策略跨实例生效 | ☐ 默认后置 |
| M-SSO 单点登录（OAuth2.0，§24） | 进公司内网、拿到 OAuth2.0 接入信息 | ☐ 设计已定稿 |
| M-Mig 迁移准备 | 迁移时点确定 | ☐ 随迁移 |
| 工单对接形态 + Phase 2 资产处置（§23.3） | 迁移时依公司平台契约 | ☐ 迁移时拍板 |
| ha（PG Cluster / Redis Sentinel） | 99.9%+ SLO / 对外 SLA | ☐ |
| 异地登录 / 验证码 / 密码过期 | 合规或安全要求 | ☐ |
| activelist 蓝图（E13/G1/G3/G4）+ 多源接入 | activelist 独立项目成型（M-A） | ☐ 随其项目 |
| 事件 L2（Outbox）/ AK-SK / 部署级分离档位 2 | 多消费者 / M2M 调用方 / 独立扩缩 | ☐ |
| 附件 / auth-enhance（2b-ext） | 按需（附件启动占用迁移号，A2 规则） | ☐ 独立窗口 |
| BranchedStateEngine / 工单业务闭环（翻案） | 公司平台无法承接 / 迁移取消（§23 翻案） | 暂缓 |

---

## 四、⚠️ 不确定项（实现前确认，13 §9）

| # | 事项 | 当前状态 |
|---|---|---|
| U1 | 03-audit-l2 写入管道：同步落库 vs Redis List 缓冲、fail-open vs fail-close | 待拍板（03 §7） |
| U-A | M-A activelist 收窄后核心功能人日（动态数据模型平台薄层，独立库 + 独立数据库） | 约 2–4（⚠️ 待校准，原单估） |
| U-B | **共享 utils 抽取**：zhuzhao `internal/pkg` → 独立共享项目（命名/范围/config 解耦/zhuzhao 全仓 import 变更） | 🚦 待所有者启动；见 ADR-003 修订节 |
| U-HR | HR 同步三拍板：离职在途工单处置 / 部门撤销级联 / 跨部门权限分配规则 | 随 M-HR 启动拍板 |
| U-SSO | 公司 OAuth2.0 接入信息（endpoint / client_id / secret）与 userinfo 字段差异（§24 字段适配层） | 进内网后获取 |
| U6 | 多实例验收环境（MI1–5 需 2 实例 + Nginx） | 随 M1 🚦 触发 |
| U7 | 迁移号最终占用（与 2b-ext 附件竞争） | A2 规则，启动时核对 |
| U8 | 启动是否翻转 roadmap「暂缓」状态 | 所有者启动时确认 |
| U9 | 日历排期（人日 → 日期） | 待给启动日 + 人力 |

---

## 五、文档就绪度矩阵（全量就绪）

| 文档 | 内容 | 状态 | 归属 |
|---|---|---|---|
| 00-startup-checklist | 启动检查单（A/B/IW 档 + 决策清单），含 §23/§24 变更记录 | 已编写 | W0 入口 |
| 01-observability / 02-multi-instance | 可观测性；Casbin Watcher + L1 消费互斥锁 + MI1–5 | 已编写 | M1（🚦） |
| 03-audit-l2 | 审计 L2 + B11① 判定日志 + B11② 归档（归档随 M-E 首任务） | 已编写 09-02 | M1 / M-E |
| 06-ha / 07-security-enhance / 08-ops / 09-platform | 高可用 / 安全增强 / 运维 / 平台增强（3b） | 已编写 09-02 | 🚦 |
| 10-ticket-business | 工单业务能力 SSOT（含 7-0 决议）——**转对接参考** | 对接参考 | M2（暂缓） |
| 11-deployment-split | 部署级分离三档 | 已编写 | 可选 |
| 12-frontend | 前端工程：BK-18 管理页/动态表单仍有效；审批/报表前端**随 M2 暂缓** | 参考规格 | IW3 + M2 |
| 13-implementation-plan | **执行计划 SSOT**：现行主链 + 🚦/⚠️ + 变更记录（§22/§23/M-E/M-SSO） | 已编写 09-02 | 主链排期 |
| ops/deployment.md | 部署文档（B10） | 已编写 09-02 | M-Mig |

---

## 六、迁移编号规划（启动时按 A2 重排）

当前占用 `000001–000019`。Phase 3 原工单规划 `000017–000021`（重排 000020 起）**随工单自研暂缓**，保留作对接参考；**现行主链**新增规划：

| 迁移（编号启动时定） | 内容 | 归属 |
|---|---|---|
| （待定，000020 起） | **job_configs + job_runs**（任务平台两表，自定义脚本任务） | M-E |
| （待定） | **判定日志表**（B11①，若随 M-E 合并实施） | M-E / M1 |
| （待定） | HR 对账字段（external_id / employee_no / domain_account，**schema 已预埋**，按表结构变更情况补迁移） | M-HR |
| （待定） | 登录审计 method 字段（SSO，§24；按实现方式定是否迁移） | M-SSO |
| （待定） | 2b-ext 附件若先启动 → 占用编号 | IW2 |
| ~~000020–000024~~ | ~~ticket_sla / notifications / workflow_* / assignment_rules / ticket_events ALTER~~ —— **随工单自研暂缓**，保留作对接参考 | M2（暂缓） |

> **A2 编号冲突规则**：谁先启动谁占用，后者整体重排。新迁移 up/down 成对、全局唯一，先核对 `docs/phase2/README §2.4` 编号占用表。

---

## 七、决策点清单（现行）

| 事项 | 状态 |
|---|---|
| §21 软删组织委托残留：BK-20 守卫（待实施）+ 已结工单委托残留登记不修 | 已拍板 |
| §22 执行结构六项：M1 降 🚦 / HR 升主链 / activelist 拆两半（后被 §23.2 取代）/ 签发模型=指派给人 / 引擎可替换约束；其中「7c 引擎硬交付」**被 §23 推翻** | 已拍板（部分被覆盖） |
| §23 重定位：工单自研暂缓、内部引擎优先、Phase 2 封版、主线改通用能力 + 内网迁移 | 已拍板 |
| §24 SSO：OAuth2.0 授权码预留接口版、鉴权三层零改动、JIT 关、本地兜底 | 已拍板（🚦 实施） |
| A2 迁移号归属 / A3 org_path 快照 | 已拍板 |
| K8s vs Compose / Redis·PG HA / 部署级分离时机 / 03 写入管道 | 待拍板 |
| 工单对接形态 + Phase 2 资产处置（§23.3） | 迁移时拍板 |
| SSO 单点登出（SLO）联动 / HR 三拍板 / M-A 人日 | 迁移 / 启动时 |

---

## 八、验收标准（现行主链）

| 档位 | 适用 | 关键指标 |
|---|---|---|
| **Phase 3-min** | 单实例、内网、低 SLA | M-E 任务平台跑通（归档 + 自定义脚本）+ M-HR mock 源对账幂等 + 内网部署演练 + 可恢复（slog+request_id、observability 关闭零开销、限流/HTTPS、PG 备份、迁移脚本化）。**工单业务不纳入验收回归**（已随 §23 暂缓） |
| **Phase 3-full** | 多实例或需 SLO / 对外 SLA | min 基础上 + 多实例 99.9% + Metrics/追踪 + Casbin Watcher + 审计 Redis List L2 + 密码策略/异地登录/API 限流 + Swagger CI + 集成测试自动化 |
| **Phase 3+**（按需） | 事件 L2 / 平台增强 / 迁移后 | Outbox + 多消费者；权限缓存跨实例失效；AK-SK（有 M2M 时）；SSO 与公司平台连通（M-SSO）；工单对接形态落地（§23.3） |

> **历史口径保留说明**：原 Wave 结构（W0 门禁 → W1 基座 → W2 工单业务 → W3 加固 → W4 activelist）与工单 TB1–16 / FE1–4 验收用例，在 §23 重定位后不再驱动排期，作为历史设计与对接参考保留于 10/12/13 §4 及各文档变更记录。

---

## 九、变更记录

| 日期 | 说明 |
|---|---|
| 2026-09-02 | 建档：§21–§24 全部拍板后，归拢 design-decisions / 13 / 00 / README 中的 Phase 3 规划为现状整理稿（现行主链 / 暂缓 / 🚦 / ⚠️ / 文档 / 迁移 / 验收 / 决策清单）；替代此前 `phase3-inventory.html` 规划素材，转 md 纳入 docs/phase3 体系 |

---

**配套索引**：启动入口 [00-startup-checklist](./00-startup-checklist.md)；排期 [13-implementation-plan](./13-implementation-plan.md)；决策 SSOT [design-decisions §21–§24](../design/design-decisions.md)。
