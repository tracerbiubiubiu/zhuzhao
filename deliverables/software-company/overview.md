# 审查交付概览（Overview）

## 本次做了什么

应要求对 **zhuzhao** 及其父目录生态（zhuzhao-utils / taskrunner / activelist / zhuzhao-ui）做了一次三维只读审查：
1. **实现能力盘点** —— 弄清当前真正落地了什么；
2. **实现 ↔ 文档一致性核对** —— 以 `docs/review/11-project-control.md` 为被核验对象，逐项与磁盘代码/迁移对账；
3. **文档设计合理性 + 业界实践符合度** —— 评估文档体系本身，并对标业界主流。

组织方式：交付总监主理 + 架构师（实现↔文档）+ 产品经理（业界对标）三条线，**只读，未修改任何项目代码/文档**。

## 主要结论

- **实现度很高、文档基本可信、方向无错。** 抽查 14 项"已修复/已实施"声明**全部有代码 + 测试双证**；迁移 25 对精确吻合；三层鉴权逐条对应。
- **确认不一致 3 条**（均为文档改字级）：① 11 号 §1/§3 回调路径自相矛盾；② 11 号 §3 误标 ticket 元数据"只读"（实有 7 写端点）；③ `standards.md` 健康检查命名与代码冲突。
- **疑似/滞后 ~8 条**：规模数字漂移、15 处失效代码路径、11 条死链、ADR-003 版本号滞后等。
- **结构性观察（经确认为预期，非缺陷）**：`doc/`（44 篇旧评估，`.gitignore` 有意忽略、不入版本控制）与 `docs/`（87 篇，现行 SSOT）两棵树定位清晰——**现行一律以 `docs/` 为准**。文档行数（25,874）> 代码行数（23,233），属"文档驱动"项目的正常体量。
- **业界对标**：六维中四维符合、两维部分符合（更轻/可收敛），**无偏离主流**；建议补 ADR、评估策略引擎收敛、迁移工具升级。
- **生态风险**：跨仓契约（activelist 路由清单、taskrunner 隐式契约、utils 版本 pin）靠人工同步，无自动校验。

## 关键文件

- **本报告（终稿）**：`deliverables/software-company/zhuzhao-audit-2026-09-11.md`
- 架构审查原始报告：`.workbuddy/review-audit/architect-report.md`
- 业界对标原始报告：`.workbuddy/review-audit/pm-benchmark-report.md`

## 后续建议与落实情况（2026-09-11 · 三轮收尾）

1. **P0 ✅ 已修**：3 条确认不一致（§1 回调路径、§3「只读」标注、standards 健康检查命名）已改。
2. **P1 ✅ 已修**：`docs/` 内失效代码路径（utils 抽取遗留，含二轮漏掉的**目录形态** `internal/pkg/{redis,response,jwt}/`）+ `docs/` 内死链/伪链已治理。
3. **P2 ✅ 已修**：规模数字校准（75/67/254）、builtin 状态改「🟡 库就绪待接线」（对齐基线记号）。
4. **P3 ⏳ 待你决策**：补 ADR（5–8 篇）、评估策略引擎收敛（OPA/Casbin model）、跨仓契约自动校验——已登记进 `docs/review/11 §8 随手项`，属触发驱动，非缺陷。
5. **三轮修正**：Owner 澄清 `doc/` 为**最初版旧文档归档（`.gitignore` 有意忽略，不入版本控制）**，**现行一律以 `docs/` 为准**。据此**回退**了第二轮对 `doc/` 的全部改动（新建的 `doc/README.md`、5 处旧仓链接修复、`docs/README.md` 归档登记块、`system-comparison.md` 数字注记），并以 `docs/` 既有惯例为基线复核通过。

> **⚠️ 需你留意**：工作区里出现一处 `.gitignore` 改动（在 `doc/` 行下方新增 `.workbuddy`、替换空行）——**并非本次审查所为**，疑似 IDE/宿主自动维护，未擅自回退，请你确认是否保留。
> **原 N-1 关闭**：`doc/` 被 `.gitignore` 忽略经确认为**有意为之**，非缺陷。

> 详细修复清单（分三轮）与"有意不改"清单见终稿报告 **§8 修复实施记录**（含 §8.4 三轮回退与一致性复核）。

## 未覆盖 / 限制

- ⚠️ **未运行** `make acceptance` 等门禁（环境无 PG/Redis），文档中"四档全绿"的运行结论未复现。
- 未逐项核对 swagger.json 与 handler 注解；未核验 compose 细节；文档为"重点全量 + 其余抽样"。
