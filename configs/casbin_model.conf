[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
# admin 角色直接放行
# 角色匹配 + 路径匹配 + 方法匹配
m = g(r.sub, "admin") || \
    (g(r.sub, p.sub) && keyMatch2(r.obj, p.obj) && r.act == p.act)
